import Counter from "@/components/count-down/counter";

export default function Home() {
  return (
    <Counter
      // states={{ onboardingComplete: false, timerIsRunning: true }}
      // Duration={1200}
      // TargetTime={new Date(2024, 10, 16, 4, 0, 0)}
    ></Counter>
  );
}
